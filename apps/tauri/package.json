{
  "name": "@deepseek-ai/dsh-tauri",
  "description": "Tauri desktop shell that boots `dsh web` (bundled runtime or repository source) and hosts the Web UI in the system webview",
  "version": "0.1.6-alpha.2",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "cargo run --manifest-path src-tauri/Cargo.toml",
    "build": "cargo build --release --manifest-path src-tauri/Cargo.toml",
    "icons": "node scripts/make-icons.mjs",
    "prepare:runtime": "node scripts/prepare-runtime.mjs",
    "package": "pnpm run prepare:runtime && tauri build --config src-tauri/tauri.packager.json"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
